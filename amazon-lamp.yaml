---
- name: Install LAMP Stack (Universal - Amazon Linux)
  hosts: webservers
  become: true

  vars:
    ansible_python_interpreter: /usr/bin/python3
    db_root_password: StrongPassword123

  tasks:

    - name: Update all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest

    - name: Install Apache
      ansible.builtin.dnf:
        name: httpd
        state: present

    # 🔥 FIX: Install correct MariaDB version automatically
    - name: Install MariaDB (try AL2 package)
      ansible.builtin.dnf:
        name: mariadb-server
        state: present
      ignore_errors: true
      register: mariadb_al2

    - name: Install MariaDB (fallback for AL2023)
      ansible.builtin.dnf:
        name: mariadb105-server
        state: present
      when: mariadb_al2.failed

    - name: Install PHP + dependencies
      ansible.builtin.dnf:
        name:
          - php
          - php-mysqlnd
          - php-fpm
          - php-json
          - php-cli
        state: present

    # 🔥 REQUIRED for MySQL Ansible module
    - name: Install PyMySQL
      ansible.builtin.dnf:
        name: python3-PyMySQL
        state: present

    - name: Start MariaDB
      ansible.builtin.service:
        name: mariadb
        state: started
        enabled: true

    - name: Set MariaDB root password
      community.mysql.mysql_user:
        name: root
        host: localhost
        password: "{{ db_root_password }}"
        login_unix_socket: /var/lib/mysql/mysql.sock
      ignore_errors: true

    - name: Create test PHP page
      ansible.builtin.copy:
        dest: /var/www/html/index.php
        content: |
          <?php
          phpinfo();
          ?>

    - name: Check Apache config
      ansible.builtin.command: apachectl configtest
      register: apache_test
      ignore_errors: true

    - name: Fail if Apache config is invalid
      ansible.builtin.fail:
        msg: "Apache config failed"
      when: apache_test.rc != 0

    - name: Start Apache
      ansible.builtin.service:
        name: httpd
        state: started
        enabled: true